import { localURL } from "./helper/localUrl"

describe('Продукт', () => {

   // beforeEach(async ({ browser }) => {
   //    await browser.url(localURL('/hw/store/catalog/1'))
   // })

   // it("должен отображать название", async ({ browser }) => {
   //    const name = await browser.$('.ProductDetails-Name');
   //    await name.isDisplayed()
   //    expect(name).toBeTruthy()
   // })

   // it('должен отображать цену', async ({ browser }) => {
   //    const price = await browser.$('.ProductDetails-Price');
   //    await price.isDisplayed()
   //    expect(price).toBeTruthy()
   // })

   // it('должен отображать описание', async ({ browser }) => {
   //    const description = await browser.$('.ProductDetails-Description');
   //    await description.isDisplayed()
   //    expect(description).toBeTruthy()
   // })

   // it('должен отображать цвет', async ({ browser }) => {
   //    const color = await browser.$('.ProductDetails-Color');
   //    await color.isDisplayed()
   //    expect(color).toBeTruthy()
   // })

   // it('должен отображать материал', async ({ browser }) => {
   //    const material = await browser.$('.ProductDetails-Material');
   //    await material.isDisplayed()
   //    expect(material).toBeTruthy()
   // })
})